apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.inventory.name }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.inventory.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.inventory.name }}
    spec:
      containers:
      - name: inventory
        image: {{ .Values.inventory.image }}
        resources:
          limits:
            memory: "{{ .Values.resources.memory }}"
            cpu: "{{ .Values.resources.cpu }}"
        ports:
        - containerPort: {{ .Values.inventory.containerPort }}
        readinessProbe:
          initialDelaySeconds: {{ .Values.healthCheck.initialDelaySeconds }}
          periodSeconds: {{ .Values.healthCheck.periodSeconds }}
          httpGet:
            path: {{ .Values.healthCheck.path }}
            port: {{ .Values.inventory.containerPort }}
        env:
        - name: inventory_URL
          valueFrom:
            configMapKeyRef:
              name: {{ .values.config.name }}
              key: {{ .Values.config.inventory }}


---
apiVersion: v1
kind: Service
metadata:
 name: {{ .Values.inventory.serviceName }}
spec:
  selector:
    app: {{ .Values.inventory.name }}
  type: NodePort
  ports:
  - port: {{ .Values.inventory.servicePort }}
    targetPort: {{ .Values.inventory.containerPort }}
    nodePort: {{ .Values.inventory.nodePort }}
